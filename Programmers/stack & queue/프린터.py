''' def solution(prior, loc):
    max_idx = prior.index(max(prior))
    
    
    while (len(prior) > 0):
        prior[i] 
    return result

print(solution([2,1,3,2], 2)) # 1
print(solution([1,1,9,1,1,1], 0)) # 1 '''

arr = [2,1,3,2]
dic = {i : int for i, int in enumerate(arr)}
print(dic)

max_idx = arr.index(max(arr))
